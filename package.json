{
  "name": "draftfortress_1",
  "version": "1.0.0",
  "description": "First Blood of coop coding 👾",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm i && cd api && npm i && cd ../client && npm i",
    "start-backend": "cd api && npm run start:dev",
    "start-client": "cd client && npx gulp",
    "start-webdev": "concurrently --kill-others \"npm:start-backend\" \"npm:start-client\""
  },
  "keywords": [],
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
